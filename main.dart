import 'package:flutter/material.dart';
import 'package:qr_scanner/ui/home.dart';

void main() {
  runApp(MaterialApp(
    title: "QR Scanner",
    home: Home(),theme: ThemeData(primaryColor: Colors.teal),
  ));
}
